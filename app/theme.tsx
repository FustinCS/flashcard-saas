'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#9FE2BF',
      },
      secondary: {
        main: '#F5F5DC'
      }
    }
  });

export default theme;